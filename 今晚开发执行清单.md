# 企业数据智能管理检索系统 — 今晚开发执行清单

## 1. 目标

- 基于 `企业数据智能管理检索系统-设计文档.md`，先落地可运行 MVP。
- 今晚优先打通链路：`Excel 导入 -> 入库 -> 企业检索 -> 健康检查`。
- 后续模块（地图/权重/AI/拓扑/匹配）按接口预留，明早可并行继续开发。

## 2. 交付物

- `backend/`：FastAPI 后端工程（可启动、可调用接口）
- `backend/app/models.py`：核心表 `company`、`import_task`
- `backend/app/routers/imports.py`：Excel 导入接口
- `backend/app/routers/companies.py`：检索接口
- `backend/app/main.py`：应用入口与健康检查
- `backend/requirements.txt`：依赖清单
- `backend/README.md`：启动说明

## 3. 今晚分工建议

### A 组（后端主链路）

- 建工程与配置
- 建表与会话管理
- 实现导入接口与状态接口
- 实现搜索接口（精准/模糊、分页）

### B 组（前端并行）

- 起 Vue3 骨架
- 检索页与导入页页面框架
- 对接 `/api/v1/companies/search` 与 `/api/v1/import/excel`

### C 组（数据与算法并行）

- 梳理字段映射词典 `field_mapping.yaml`
- 准备行业权重数据源与参数配置草案

## 4. 今晚验收（MVP）

- 能上传 `xlsx`
- 数据写入 `company.raw_data`
- 可通过关键词+城市+行业查询
- 分页返回稳定
- `/healthz` 返回服务状态

## 5. 次阶段待接入（接口预留）

- `/api/v1/map/companies`
- `/api/v1/regions/{city}/industry-weights`
- `/api/v1/topology/nearest`
- `/api/v1/match/prioritize`
- `/api/v1/ai/company`

## 6. 风险与处理

- Excel 字段杂乱：统一 `raw_data` 兜底，不阻塞导入。
- 地图与新闻 API 限流：加入 Redis 缓存与重试策略。
- 模糊检索慢：先 `LIKE`，后续升级 `pg_trgm`。
